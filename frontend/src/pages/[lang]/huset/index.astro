---
import HousePage from "../../../pageviews/HousePage.astro";
import type { Lang } from "../../../lib/i18n";

export function getStaticPaths() {
  return [{ params: { lang: "en" } }, { params: { lang: "de" } }];
}

const { lang } = Astro.params;
---

<HousePage lang={lang as Lang} />
