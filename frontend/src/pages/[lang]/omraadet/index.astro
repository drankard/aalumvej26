---
import AreasIndex from "../../../pageviews/AreasIndex.astro";
import type { Lang } from "../../../lib/i18n";

export function getStaticPaths() {
  return [{ params: { lang: "en" } }, { params: { lang: "de" } }];
}

const { lang } = Astro.params;
---

<AreasIndex lang={lang as Lang} />
