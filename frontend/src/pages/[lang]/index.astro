---
import HomePage from "../../pageviews/HomePage.astro";
import type { Lang } from "../../lib/i18n";

export function getStaticPaths() {
  return [{ params: { lang: "en" } }, { params: { lang: "de" } }];
}

const { lang } = Astro.params;
---

<HomePage lang={lang as Lang} />
