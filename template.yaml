AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: Aalumvej26 - React + Lambda + DynamoDB

Globals:
  Function:
    Timeout: 30
    Runtime: python3.12
    MemorySize: 256

Parameters:
  Stage:
    Type: String
    Default: prod
  ContentAgentCodeBucket:
    Type: String
    Description: S3 bucket containing the content agent code zip
    Default: ''
  ContentAgentCodeKey:
    Type: String
    Description: S3 key of the content agent code zip
    Default: ''

Conditions:
  HasAgentCode: !Not [!Equals [!Ref ContentAgentCodeBucket, '']]

Resources:
  # AgentCore Execution Role (created only when agent code is provided)
  ContentAgentExecutionRole:
    Type: AWS::IAM::Role
    Condition: HasAgentCode
    Properties:
      RoleName: !Sub aalumvej26-content-agent-role-${Stage}
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: bedrock-agentcore.amazonaws.com
            Action: sts:AssumeRole
      Policies:
        - PolicyName: ContentAgentPolicy
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - dynamodb:GetItem
                  - dynamodb:PutItem
                  - dynamodb:Query
                  - dynamodb:DeleteItem
                Resource: !GetAtt AppTable.Arn
              - Sid: BedrockModelInvocation
                Effect: Allow
                Action:
                  - bedrock:InvokeModel
                  - bedrock:InvokeModelWithResponseStream
                Resource:
                  - 'arn:aws:bedrock:*::foundation-model/*'
                  - !Sub 'arn:aws:bedrock:${AWS::Region}:${AWS::AccountId}:*'
                  - 'arn:aws:bedrock:*:*:inference-profile/*'
              - Sid: MarketplaceAccess
                Effect: Allow
                Action:
                  - aws-marketplace:ViewSubscriptions
                  - aws-marketplace:Subscribe
                Resource: '*'
              - Sid: BedrockPromptAccess
                Effect: Allow
                Action:
                  - bedrock:GetPrompt
                Resource:
                  - !Sub 'arn:aws:bedrock:${AWS::Region}:${AWS::AccountId}:prompt/*'
              - Sid: SSMAccess
                Effect: Allow
                Action:
                  - ssm:GetParameter
                Resource: !Sub 'arn:aws:ssm:${AWS::Region}:${AWS::AccountId}:parameter/aalumvej26/*'
              - Sid: CloudWatchLogsGroup
                Effect: Allow
                Action:
                  - logs:CreateLogGroup
                  - logs:DescribeLogStreams
                  - logs:DescribeLogGroups
                Resource:
                  - !Sub 'arn:aws:logs:${AWS::Region}:${AWS::AccountId}:log-group:/aws/bedrock-agentcore/runtimes/*'
                  - !Sub 'arn:aws:logs:${AWS::Region}:${AWS::AccountId}:log-group:*'
              - Sid: CloudWatchLogsStream
                Effect: Allow
                Action:
                  - logs:CreateLogStream
                  - logs:PutLogEvents
                Resource:
                  - !Sub 'arn:aws:logs:${AWS::Region}:${AWS::AccountId}:log-group:/aws/bedrock-agentcore/runtimes/*:log-stream:*'

  # AgentCore Content Runtime (created only when agent code is provided)
  ContentAgentRuntime:
    Type: AWS::BedrockAgentCore::Runtime
    Condition: HasAgentCode
    Properties:
      AgentRuntimeName: !Sub aalumvej26_content_agent_${Stage}
      Description: Aalumvej26 content discovery and publishing agent
      AgentRuntimeArtifact:
        CodeConfiguration:
          Code:
            S3:
              Bucket: !Ref ContentAgentCodeBucket
              Prefix: !Ref ContentAgentCodeKey
          EntryPoint:
            - agent.py
          Runtime: PYTHON_3_12
      RoleArn: !GetAtt ContentAgentExecutionRole.Arn
      EnvironmentVariables:
        AGENT_NAME: Aalumvej26 Content Agent
        AWS_REGION: !Ref AWS::Region
        TABLE_NAME: !Ref AppTable
        MODEL_ID_PARAM: /aalumvej26/ai/model-id
        BASE_PROMPT_ID: !Sub '{{resolve:ssm:/aalumvej26/prompts/base-system-arn}}'
        OPLEVELSER_PROMPT_ID: !Sub '{{resolve:ssm:/aalumvej26/prompts/oplevelser-arn}}'
        OMRAADET_PROMPT_ID: !Sub '{{resolve:ssm:/aalumvej26/prompts/omraadet-arn}}'
      LifecycleConfiguration:
        IdleRuntimeSessionTimeout: 300
        MaxLifetime: 3600
      NetworkConfiguration:
        NetworkMode: PUBLIC
      ProtocolConfiguration: HTTP

  AppTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: !Sub aalumvej26-${Stage}
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: pk
          AttributeType: S
        - AttributeName: sk
          AttributeType: S
      KeySchema:
        - AttributeName: pk
          KeyType: HASH
        - AttributeName: sk
          KeyType: RANGE

  AppFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub aalumvej26-${Stage}
      CodeUri: backend/
      Handler: handler.lambda_handler
      Environment:
        Variables:
          TABLE_NAME: !Ref AppTable
          STAGE: !Ref Stage
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref AppTable
      Events:
        RpcEndpoint:
          Type: HttpApi
          Properties:
            ApiId: !Ref AppApi
            Path: /rpc
            Method: POST

  AppApi:
    Type: AWS::Serverless::HttpApi
    Properties:
      StageName: !Ref Stage
      CorsConfiguration:
        AllowOrigins:
          - "*"
        AllowMethods:
          - POST
          - OPTIONS
        AllowHeaders:
          - Content-Type

  # Content Agent Invoker Lambda
  ContentAgentInvokerFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub aalumvej26-content-invoker-${Stage}
      CodeUri: backend/lambdas/content_agent_invoker/
      Handler: handler.lambda_handler
      Timeout: 900
      MemorySize: 256
      Environment:
        Variables:
          TABLE_NAME: !Ref AppTable
          STAGE: !Ref Stage
          CONTENT_RUNTIME_ARN: !If [HasAgentCode, !GetAtt ContentAgentRuntime.AgentRuntimeArn, '']
          BASE_PROMPT_ID: !Sub '{{resolve:ssm:/aalumvej26/prompts/base-system-arn}}'
          OPLEVELSER_PROMPT_ID: !Sub '{{resolve:ssm:/aalumvej26/prompts/oplevelser-arn}}'
          OMRAADET_PROMPT_ID: !Sub '{{resolve:ssm:/aalumvej26/prompts/omraadet-arn}}'
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref AppTable
        - Statement:
            - Effect: Allow
              Action:
                - bedrock-agentcore:InvokeAgentRuntime
              Resource: '*'
        - Statement:
            - Effect: Allow
              Action:
                - bedrock:GetPrompt
              Resource: '*'

  OplevelsSchedule:
    Type: AWS::Events::Rule
    Properties:
      Name: !Sub aalumvej26-oplevelser-schedule-${Stage}
      ScheduleExpression: 'cron(0 0 ? * SUN *)'
      State: ENABLED
      Targets:
        - Arn: !GetAtt ContentAgentInvokerFunction.Arn
          Id: OplevelsTarget
          Input: '{"pipeline": "oplevelser"}'

  OplevelsSchedulePermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref ContentAgentInvokerFunction
      Action: lambda:InvokeFunction
      Principal: events.amazonaws.com
      SourceArn: !GetAtt OplevelsSchedule.Arn

  OmraadetSchedule:
    Type: AWS::Events::Rule
    Properties:
      Name: !Sub aalumvej26-omraadet-schedule-${Stage}
      ScheduleExpression: 'cron(0 6 ? * 2#1 *)'
      State: ENABLED
      Targets:
        - Arn: !GetAtt ContentAgentInvokerFunction.Arn
          Id: OmraadetTarget
          Input: '{"pipeline": "omraadet"}'

  OmraadetSchedulePermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref ContentAgentInvokerFunction
      Action: lambda:InvokeFunction
      Principal: events.amazonaws.com
      SourceArn: !GetAtt OmraadetSchedule.Arn

  ContentNotifierFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub aalumvej26-content-notifier-${Stage}
      CodeUri: backend/lambdas/content_notifier/
      Handler: handler.lambda_handler
      Timeout: 30
      Environment:
        Variables:
          TABLE_NAME: !Ref AppTable
          SNS_TOPIC_ARN: !Ref ContentSummaryTopic
      Policies:
        - DynamoDBReadPolicy:
            TableName: !Ref AppTable
        - Statement:
            - Effect: Allow
              Action:
                - sns:Publish
              Resource: !Ref ContentSummaryTopic

  OplevelsNotifySchedule:
    Type: AWS::Events::Rule
    Properties:
      Name: !Sub aalumvej26-oplevelser-notify-${Stage}
      ScheduleExpression: 'cron(10 0 ? * SUN *)'
      State: ENABLED
      Targets:
        - Arn: !GetAtt ContentNotifierFunction.Arn
          Id: OplevelsNotifyTarget
          Input: '{"pipeline": "oplevelser"}'

  OplevelsNotifyPermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref ContentNotifierFunction
      Action: lambda:InvokeFunction
      Principal: events.amazonaws.com
      SourceArn: !GetAtt OplevelsNotifySchedule.Arn

  OmraadetNotifySchedule:
    Type: AWS::Events::Rule
    Properties:
      Name: !Sub aalumvej26-omraadet-notify-${Stage}
      ScheduleExpression: 'cron(16 6 ? * 2#1 *)'
      State: ENABLED
      Targets:
        - Arn: !GetAtt ContentNotifierFunction.Arn
          Id: OmraadetNotifyTarget
          Input: '{"pipeline": "omraadet"}'

  OmraadetNotifyPermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref ContentNotifierFunction
      Action: lambda:InvokeFunction
      Principal: events.amazonaws.com
      SourceArn: !GetAtt OmraadetNotifySchedule.Arn

  ContentSummaryTopic:
    Type: AWS::SNS::Topic
    Properties:
      TopicName: !Sub aalumvej26-content-summary-${Stage}
      DisplayName: Aalumvej26 Content Pipeline

  ContentSummarySubscription:
    Type: AWS::SNS::Subscription
    Properties:
      TopicArn: !Ref ContentSummaryTopic
      Protocol: email
      Endpoint: david@graveyard.dk

  FrontendBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub aalumvej26-frontend-${Stage}
      WebsiteConfiguration:
        IndexDocument: index.html
        ErrorDocument: index.html
      PublicAccessBlockConfiguration:
        BlockPublicAcls: false
        BlockPublicPolicy: false
        IgnorePublicAcls: false
        RestrictPublicBuckets: false

  FrontendBucketPolicy:
    Type: AWS::S3::BucketPolicy
    Properties:
      Bucket: !Ref FrontendBucket
      PolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal: '*'
            Action: s3:GetObject
            Resource: !Sub ${FrontendBucket.Arn}/*

Outputs:
  ApiUrl:
    Value: !Sub https://${AppApi}.execute-api.${AWS::Region}.amazonaws.com/${Stage}
  WebsiteUrl:
    Value: !GetAtt FrontendBucket.WebsiteURL
  FrontendBucket:
    Value: !Ref FrontendBucket
  TableName:
    Value: !Ref AppTable
  ContentAgentRuntimeArn:
    Condition: HasAgentCode
    Value: !GetAtt ContentAgentRuntime.AgentRuntimeArn
  ContentAgentRuntimeId:
    Condition: HasAgentCode
    Value: !Ref ContentAgentRuntime
